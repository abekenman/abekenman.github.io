var start = { interval, value, first};
onmessage = m => {
  if(m.data.title == 'start') start.interval = setInterval(() => 
    if(start.first != e.data.value.data || start.value.first + 1 != e.data.value.data) postMessage({ title: 'start', value: start.value = e.data.value.data + 1 })
    else postMessage({ title: 'start', value: start.value++ }), m.data.value.fps)
}
